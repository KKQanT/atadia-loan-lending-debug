import {FC} from 'react'
import { DiscordUser } from "utils/types";

interface Props {
  user: DiscordUser;
}

export const SubmitLend: FC<Props> = (props) => {
  return (
    <div>SubmitLend</div>
  )
}